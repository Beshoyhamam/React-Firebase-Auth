const Loading = () => {
    return (
        <div style={{ textAlign: "center", marginTop: "20%", fontSize: "20px" }}>
            <h2>Loading.....</h2>
        </div>
    );
}

export default Loading;